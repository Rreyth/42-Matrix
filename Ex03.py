from Vector import *

# EX03: Dot product
print("-------Vector dot product-------")

v = Vector([0, 0])
w = Vector([1, 1])

print(f"v = {v}\nw = {w}")
print(f"v.dot(w) = {v.dot(w)}")

v = Vector([1, 1])
w = Vector([1, 1])

print(f"\nv = {v}\nw = {w}")
print(f"v.dot(w) = {v.dot(w)}")

v = Vector([-1, 6])
w = Vector([3, 2])

print(f"\nv = {v}\nw = {w}")
print(f"v.dot(w) = {v.dot(w)}")